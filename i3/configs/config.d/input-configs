# Input configuration (get with swaymsg -t get_inputs)
input type:touchpad {
    dwt enabled
    tap enabled
    natural_scroll enabled
    middle_emulation enabled
}

# Set keyboard layout to Portuguese:
input type:keyboard {
    xkb_layout pt
}
